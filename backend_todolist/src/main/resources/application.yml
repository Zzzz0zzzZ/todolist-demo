spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    下面三项若使用请自行配置
    url:
    username:
    password:
  main:
    banner-mode: off

server:
  port: 8060

mybatis-plus:
  global-config:
    banner: false